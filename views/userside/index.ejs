<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Montserrat:wght@500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>Service Providers</title>
    <style>
        body {
            background: white;
            color: #0D1B2A;
            font-family: 'Roboto', sans-serif;
        }
        .profile-card {
            border: none;
            margin-bottom: 2rem;
            max-width: 350px; 
            margin: 0 auto;  
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            border-radius: 1rem;
            overflow: hidden;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .profile-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
        }

        .card-title{
            font-family: 'Montserrat', sans-serif;
            font-weight: bold;
        }
        .card-img-top {
            height: 200px;
            object-fit: cover;
        }
        .navbar {
            width: 100%;
            background: #1F3B4D; /* Deep Navy */
            color: #F5F5F5; /* Off White for contrast */
            padding: 10px 0;
            text-align: center;
            font-family: 'Playfair Display', serif;
        }
        .navbar-brand {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            color: #c89f18 !important; /* Accent Color */
            font-weight: bold;
            transition: color 0.3s ease;
        }
        .navbar-brand:hover {
            color: #1F3B4D !important; /* Primary Color */
        }


        .btn-primary {
            background: linear-gradient(45deg,  #6a11cb, #2575fc);
            border: none;
        }

        .btn-primary:hover{
            background: linear-gradient(45deg, #b522ef, #fa6b60);
            color: #fff;
        }

        .sidebar {
            position: sticky;
            height: 95vh;
            width: 22rem;
            border-radius: 1rem;
            background: #1F3B4D;
            color: #fff;
            padding: 1rem;
        }

        .sidebar a:hover {
            background: linear-gradient(45deg, #b522ef, #fa6b60);
            color: #fff;
        }

        .filter-container {
            border: 1px solid #e0e0e0;
            border-radius: 0.5rem;
            padding: 30px;
            width: 18rem;
            background-color: #ffffff;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .form-select {
            width: 100%;
        }

        .container {
            padding: 2rem 0;
        }

        @media (max-width: 768px) {
            nav {
                flex-direction: column;
                align-items: center;
            }

            .sidebar {
                width: 100%;
                margin-bottom: 2rem;
                height: auto;
            }
        }

        .filter-btn {
            background: linear-gradient(45deg, #ffe259, #ffa751);
            color: #fff;
        }

        .dropdown-toggle {
            transition: transform 0.2s ease-in-out;
        }

        .dropdown-toggle:hover {
            transform: scale(1.05);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .profile-card {
            animation: fadeInUp 0.6s ease-in-out;
        }

        .btn {
            transition: background-color 0.3s, color 0.3s, transform 0.3s;
        }

        .btn:hover {
            transform: scale(1.05);
            color: white;
        }
        /* Footer */
        footer {
            background: #1F3B4D; /* Primary Color */
            color: #F5F5F5;
            padding: 40px 0;
            text-align: center;
        }

        footer h5 {
            color: #D4AF37;
            font-family: 'Playfair Display', serif;
            margin-bottom: 20px;
        }

        footer p {
            font-size: 1rem;
        }

        footer ul {
            padding: 0;
            list-style-type: none;
        }

        footer ul li {
            font-size: 1.1rem;
            margin-bottom: 10px;
            color: #F5F5F5;
        }

        footer ul li i {
            margin-right: 10px;
            color: #D4AF37;
        }

        footer ul li a {
            color: #F5F5F5;
            text-decoration: none;
            transition: color 0.3s;
        }

        footer ul li a:hover {
            color: #D4AF37;
        }

        /* Social Media Icons */
        footer ul.list-inline {
            margin: 0;
            padding: 0;
        }

        footer ul.list-inline li {
            margin: 0 10px;
        }

        footer ul.list-inline li img {
            width: 40px;
            transition: transform 0.3s ease;
        }

        footer ul.list-inline li img:hover {
            transform: scale(1.1);
        }

        @media (max-width: 768px) {
            footer .text-md-start {
                text-align: center;
            }

            footer .col-md-4 {
                margin-bottom: 30px;
            }

            footer ul.list-inline {
                text-align: center;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#"><b>Epic Engineers</b></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="me-auto"></div>
                <div>
                    <% if (!currentUser) { %>
                    <div class="dropdown">
                        <button class="btn btn-outline-primary dropdown-toggle" type="button" id="loginDropdown"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            Login
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="loginDropdown">
                            <li><a class="dropdown-item" href="/user/login">User Login</a></li>
                            <li><a class="dropdown-item" href="/services/login">Service Provider Login</a></li>
                        </ul>
                    </div>

                    <div class="dropdown">
                        <button class="btn btn-outline-primary dropdown-toggle" type="button" id="signupDropdown"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            Sign Up
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="signupDropdown">
                            <li><a class="dropdown-item" href="/user/signup">User Sign Up</a></li>
                            <li><a class="dropdown-item" href="/services/signup">Service Provider Sign Up</a></li>
                        </ul>
                    </div>
                    <% } else { %>

                    <a href="/logout" class="btn btn-outline-primary">
                        <i class="bi bi-box-arrow-right"></i> Logout
                    </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <div class="col-md-3 mb-3 mt-3 sidebar">
                <h4 class="text-center">Hello XYZ !</h4>
                <div class="d-flex flex-column align-items-center">
                    <a href="/user/<%= currentUser._id %>/bookings" class="btn filter-btn mb-2">My Bookings</a>
                    <a href="/mapbox" class="btn filter-btn mb-2">See On Map</a>
                    <!-- Display the filter-->
                    <div class="filter-container">
                        <form action="/user/filter" method="post" class="p-3">
                            <div class="mb-3">
                                <label for="serviceType" class="form-label">Select Service</label>
                                <select class="form-select" id="serviceType" name="serviceCategory">
                                    <option value="" selected>Choose...</option>
                                    <option value="Plumber">Plumber</option>
                                    <option value="Electrician">Electrician</option>
                                    <option value="Painter">Painter</option>
                                    <option value="Beauty">Beauty</option>
                                    <option value="Nursing Home">Nursing Home</option>
                                </select>
                            </div>
                    
                            <div class="mb-3">
                                <label for="priceRange" class="form-label">Price Range</label>
                                <select class="form-select" id="priceRange" name="price">
                                    <option value="">All Prices</option>
                                    <option value="1000">Up To 1000</option>
                                    <option value="2000">Up To 2000</option>
                                    <option value="3000">Up To 3000</option>
                                    <option value="4000">Up To 4000</option>
                                </select>
                            </div>
                    
                            <div class="mb-3">
                                <label for="state" class="form-label">State</label>
                                <select class="form-select" id="state" name="state" required>
                                    <option value="" selected>Choose...</option>
                                    <% for(provider of allServiceProviders){ %>
                                        <option value="<%= provider.serviceArea.state %>"><%= provider.serviceArea.state %></option>
                                    <% } %>
                                </select>
                            </div>
                    
                            <div class="mb-3">
                                <label for="serviceArea" class="form-label">City</label>
                                <select class="form-select" id="serviceArea" name="city" required>
                                    <option value="" selected>Choose...</option>
                                    <% for(provider of allServiceProviders){ %>
                                        <option value="<%= provider.serviceArea.city %>"><%= provider.serviceArea.city %></option>
                                    <% } %>
                                </select>
                            </div>
                    
                            <button id="filterBtn" class="btn btn-primary mt-2">Apply Filter</button>
                        </form>
                    </div>                    
                </div>
            </div>

            <div class="col-md-9">
                <div class="row row-cols-1 row-cols-md-2 mt-2 row-cols-lg-3 g-4">
                    <% for (let service of services) { %>
                    <div class="col">
                        <div class="profile-card card h-65 mt-3">
                            <img src="<%= service.profileImage %>" class="card-img-top" alt="Profile">
                            <div class="card-body">
                                <h5 class="card-title"><i class="fas fa-tools"></i> <%= service.serviceCategory %></h5>
                                <p class="card-text">
                                    <%= service.username %> <br>
                                    <%= service.serviceArea.city %>,<%= service.serviceArea.state %>
                                </p>
                                <a href="/services/<%= service._id %>" class="btn btn-primary">View Profile</a>
                            </div>
                        </div>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row text-center text-md-start">
                <!-- Company Info -->
                <div class="col-md-4 mb-4">
                    <h5>Epic Engineers</h5>
                    <p>Your trusted service partner for plumbing, electrical, and home maintenance services. We ensure quality work with professional expertise.</p>
                </div>

                <!-- Contact Info -->
                <div class="col-md-4 mb-4">
                    <h5>Contact Us</h5>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-phone-alt"></i> +91 1234567890</li>
                        <li><i class="fas fa-envelope"></i> info@epicengineers.com</li>
                        <li><i class="fas fa-map-marker-alt"></i> 123, Main Street, Kolhapur, India</li>
                    </ul>
                </div>

                <!-- Social Media Links -->
                <div class="col-md-4 mb-4">
                    <h5>Follow Us</h5>
                    <ul class="list-inline">
                        <li class="list-inline-item">
                            <a href="https://www.instagram.com" target="_blank">
                                <img src="https://img.icons8.com/fluent/48/000000/instagram-new.png" alt="Instagram">
                            </a>
                        </li>
                        <li class="list-inline-item">
                            <a href="https://www.linkedin.com" target="_blank">
                                <img src="https://img.icons8.com/fluent/48/000000/linkedin.png" alt="LinkedIn">
                            </a>
                        </li>
                        <li class="list-inline-item">
                            <a href="https://wa.me/911234567890" target="_blank">
                                <img src="https://img.icons8.com/fluent/48/000000/whatsapp.png" alt="WhatsApp">
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
            <!-- Copyright Section -->
            <div class="text-center mt-4">
                <p>&copy; 2024 Epic Engineers. All Rights Reserved.</p>
            </div>
        </div>
    </footer>
    <script>
        document.getElementById("year").innerText = new Date().getFullYear();
    </script>    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
