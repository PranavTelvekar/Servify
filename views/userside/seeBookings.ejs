<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bookings</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            background: linear-gradient(120deg, #570168, #e071f9);
            color: #333;
        }

        /* Fixed Sidebar Styles */
        .sidebar {
            position: fixed;
            top: 80px;
            left: 0;
            height: 70vh; /* Full viewport height */
            width: 20rem;
            border-radius: 1rem;
            background: linear-gradient(120deg, #4febf7, #a332f3);
            color: white;
            padding: 2rem 1rem;
            overflow-y: auto; /* Sidebar scrollable if content exceeds viewport */
            z-index: 1000; /* Ensure sidebar is above content */
        }

        .sidebar h4, .sidebar a {
            text-align: center;
        }

        .sidebar a {
            color: white;
            display: block;
            margin-bottom: 1.5rem;
        }

        .sidebar a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }
        .btn-primary {
            background: linear-gradient(45deg,  #6a11cb, #2575fc);
            border: none;
        }

        .btn-primary:hover{
            background: linear-gradient(45deg, #b522ef, #fa6b60);
            color: #fff;
        }

        /* Main content styles */
        /* .content {
            margin-left: 22rem; /* Add margin to make room for the fixed sidebar */
            /* padding: 2rem; */
         */

        

        /* Navbar styles */
        nav {
            padding: 1rem 0;
        }

        .navbar {
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            background-color: #343a40;
        }

        .navbar-brand {
            height: 50px;
            width: 165px;
            text-align: center;
            align-items: center;
            border-radius: 1rem;
            background-color: #007bff;
            color: white;
        }

        .navbar-brand:hover {
            background-color: #0056b3;
            color: rgb(233, 214, 214);
        }

        .btn-primary {
            background-color: #c643fa;
            border: none;
        }

        /* Footer styles */
        footer {
            background-color: #343a40;
            color: white;
            padding: 1rem 0;
            margin-bottom: 0px;
            /* bottom: 0; */
            width: 100%;
        }

        /* Media queries */
        @media (max-width: 768px) {
            .content {
                margin-left: 0;
            }

            .sidebar {
                position: static;
                width: 100%;
                height: auto;
            }

            .sidebar a {
                text-align: left;
            }
        }

        .dropdwon{
            margin-left: 30rem;
        }

        /* Main content styles */
        .content {
            margin-left: 22rem; /* Add margin to make room for the fixed sidebar */
            padding: 2rem;
        }

        .booking-card {
            border: none;
            margin-bottom: 2rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s;
            max-height: 400px; /* Set a max height for each card */
            overflow-y: auto; /* Enable scrolling within the card */
            padding: 1.5rem; /* Original padding */
            background-color: white;
            border-radius: 1rem;
            width: 100%; /* Ensures card takes full width of its parent */
        }

        @media (max-width: 768px) {
            .booking-card {
                margin: 0 auto; /* Center card on smaller screens */
            }
        }


        .booking-card:hover {
            transform: scale(1.05);
        }

        /* Slot Details Section as a sub-point */
        .slot-details {
            margin-top: 1rem;
            padding-left: 1.5rem;
            border-left: 3px solid #007bff; /* Highlighted border for slot details */
        }

        .slot-details h4 {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
            color: #007bff; /* Change color to make it stand out */
        }

        .slot-details p {
            margin-bottom: 0.25rem;
        }

        .btn {
            transition: background-color 0.3s, color 0.3s, transform 0.3s;
        }

        .btn:hover {
            transform: scale(1.05);
            color: white;
        }
        .bookingBtn{
            width: 10rem;
            background: linear-gradient(45deg, #ffe259, #ffa751);
            color: #fff;
        }

        footer {
            background: linear-gradient(120deg, #6a11cb, #2575fc); 
            color: white; 
            padding: 40px 20px; 
            text-align: center; 
            position: relative; 
        }

        footer a {
            color: white;
            text-decoration: none; 
            transition: color 0.3s; 
        }

        footer a:hover {
            color: #f7b733; 
        }

        footer .social-icons {
            margin: 20px 0; 
        }

        footer .social-icons a {
            margin: 0 10px; 
            font-size: 24px; 
        }

        footer .quick-links {
            margin: 20px 0; 
        }

        footer .quick-links a {
            margin: 0 15px; 
            font-weight: bold; 
        }

        footer form {
            margin: 20px 0;
        }

        footer form input[type="email"] {
            padding: 10px; 
            border: none; 
            border-radius: 10rem; 
            outline: none;
        }

        footer form button {
            background-color: #f7b733; 
            color: white; 
            border: none; 
            border-radius: 20px; 
            padding: 10px 20px; 
            margin-left: 10px; 
            cursor: pointer; 
            transition: background-color 0.3s; 
        }

        footer form button:hover {
            background-color: #ffdd00; /* Change button color on hover */
        }

        footer .copyright {
            margin-top: 20px; /* Margin for spacing */
            font-size: 14px; /* Font size for copyright text */
        }
        .alert-success{
            display: none;
        }
        .alert-danger{
            display: none;
        }
    </style>
</head>
<body>
    <div class="alert alert-success" id="successBox" role="alert">
        <%= success %>
    </div>
    <div class="alert alert-danger" id="errorBox" role="alert">
        <%= error %>
      </div>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#"><b>Epic Engineers</b></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <div class="me-auto"></div>
                <div>
                    <% if (!currentUser) { %>
                    <div class="dropdown">
                        <button class="btn btn-outline-primary dropdown-toggle" type="button" id="loginDropdown"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            Login
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="loginDropdown">
                            <li><a class="dropdown-item" href="/user/login">User Login</a></li>
                            <li><a class="dropdown-item" href="/services/login">Service Provider Login</a></li>
                        </ul>
                    </div>

                    <div class="dropdown">
                        <button class="btn btn-outline-primary dropdown-toggle" type="button" id="signupDropdown"
                            data-bs-toggle="dropdown" aria-expanded="false">
                            Sign Up
                        </button>
                        <ul class="dropdown-menu" aria-labelledby="signupDropdown">
                            <li><a class="dropdown-item" href="/user/signup">User Sign Up</a></li>
                            <li><a class="dropdown-item" href="/services/signup">Service Provider Sign Up</a></li>
                        </ul>
                    </div>
                    <% } else { %>
                    <a href="/logout" class="btn btn-outline-primary">
                        <i class="bi bi-box-arrow-right"></i> Logout
                    </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 sidebar">
                <h4>Hello <%= currentUser.username %>!</h4>

                <!-- Dropdown for Filter Profiles -->
                <div class="dropdown mb-3">
                    <button class="btn filter-btn dropdown-toggle mt-2" type="button" id="filterDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        Filter Profiles!
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="filterDropdown">
                        <li>
                            <form action="/user/filter" method="post" class="p-3">
                                <div class="mb-3">
                                    <label for="serviceType" class="form-label">Select Service</label>
                                    <select class="form-select" id="serviceType" name="serviceCategory">
                                        <option value="" selected>Choose...</option>
                                        <option value="Plumber">Plumber</option>
                                        <option value="Electrician">Electrician</option>
                                        <option value="Painter">Painter</option>
                                        <option value="Beauty">Beauty</option>
                                        <option value="Nursing Home">Nursing Home</option>
                                    </select>
                                </div>

                                <div class="mb-3">
                                    <label for="priceRange" class="form-label">Price Range</label>
                                    <select class="form-select" id="priceRange" name="price">
                                        <option value="">All Prices</option>
                                        <option value="1000">Up To 1000</option>
                                        <option value="1500">Up To 1500</option>
                                        <option value="2000">Up To 2000</option>
                                        <option value="2500">Up To 2500</option>
                                    </select>
                                </div>

                                <div class="mb-3">
                                    <label for="state" class="form-label">State</label>
                                    <select class="form-select" id="state" name="state" required>
                                        <option value="" selected>Choose...</option>
                                        <% for(provider of allServiceProviders){ %>
                                            <option value="<%= provider.serviceArea.state %>"><%= provider.serviceArea.state %></option>
                                        <% } %>
                                    </select>
                                </div>
                                <div class="mb-3">
                                    <label for="serviceArea" class="form-label">City</label>
                                    <select class="form-select" id="serviceArea" name="city" required>
                                        <option value="" selected>Choose...</option>
                                        <% for(provider of allServiceProviders){ %>
                                            <option value="<%= provider.serviceArea.city %>"><%= provider.serviceArea.city %></option>
                                        <% } %>
                                    </select>
                                </div>

                                <button id="filterBtn" class="btn btn-primary mt-2">Apply Filter</button>
                            </form>
                        </li>
                    </ul>
                </div>

                <!-- Dropdown for Booking Status -->
                <div class="dropdown mb-3">
                    <button class="btn filter-btn dropdown-toggle" type="button" id="statusDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        Booking Status
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="statusDropdown">
                        <li>
                            <form action="/user/<%= currentUser._id %>/filteByStatus" method="post" class="p-3">
                                <select class="form-select mb-3" name="bookingStatus">
                                    <option value="" selected>Choose Status...</option>
                                    <option value="Confirm">Confirm</option>
                                    <option value="Cancel">Cancel</option>
                                    <option value="Pending">Pending</option>
                                </select>
                                <button class="btn btn-primary">Apply</button>
                            </form>
                        </li>
                    </ul>
                </div>
                <div class="dropdown mb-3">
                    <button class="btn filter-btn dropdown-toggle" type="button" id="priceDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        Slot Price
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="priceDropdown">
                        <li>
                            <form action="/user/<%= currentUser._id %>/filteBySlotPrice" method="post" class="p-3">
                                <select class="form-select mb-3" name="slotPrice">
                                    <option value="" selected>Choose Price...</option>
                                    <option value="250">Up To 250</option>
                                    <option value="500">Up To 500</option>
                                    <option value="750">Up To 750</option>
                                    <option value="1000">Up To 1000</option>
                                </select>
                                <button class="btn btn-primary">Apply</button>
                            </form>
                        </li>
                    </ul>
                </div>
            </div>

            <!-- Main Content Area -->
            <!-- Main content with booking details -->
            <div class="col-md-9 content">
                <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 mt-3">
                    <% for (booking of bookings) { %>
                    <div class="col d-flex justify-content-center"> <!-- Added flexbox class -->
                        <div class="booking-card">
                            <h3>Booking Details</h3>
                            <p><strong>Booking Date:</strong> <%= new Date(booking.bookingDate).toLocaleDateString() %></p>
                            <p><strong>Problem Description:</strong> <%= booking.problemDescription %></p>
                            <p><strong>Booking Status:</strong> <%= booking.status %></p>
                            
                            <!-- Slot Details as sub-points -->
                            <div class="slot-details">
                                <h4>Slot Details</h4>
                                <p><strong>Slot Date:</strong> <%= new Date(booking.slot.date).toLocaleDateString() %></p>
                                <p><strong>Slot Start Time:</strong> <%= booking.slot.startTime %></p>
                                <p><strong>Slot End Time:</strong> <%= booking.slot.endTime %></p>
                                <p><strong>Slot Price:</strong> ₹<%= booking.slot.price %></p>
                            </div>
                            <br>
                            <% if(booking.cancelBefore.getTime()>Date.now()){ %>
                                <form action="/user/<%= booking._id %>/bookingCancel" method="get">
                                    <button>Cancel Booking</button>
                                </form>
                            <% } %>
                        </div>
                    </div>
                    <% } %>
                </div>
            </div>
            
        </div>
    </div>

    <footer>
        <div class="social-icons">
            <a href="https://www.facebook.com" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
            <a href="https://www.twitter.com" aria-label="Twitter"><i class="bi bi-twitter"></i></a>
            <a href="https://www.instagram.com" aria-label="Instagram"><i class="bi bi-instagram"></i></a>
        </div>
        
        <div class="quick-links">
            <a href="/about">About Us</a>
            <a href="/contact">Contact</a>
            <a href="/privacy">Privacy Policy</a>
        </div>
    
        <form>
            <input type="email" placeholder="Enter your email" aria-label="Email Subscription">
            <button type="submit">Subscribe</button>
        </form>
    
        <div class="copyright">
            &copy; <span id="year"></span> Epic Engineers. All rights reserved.
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
    <script>
        const error = "<%= error %>"; // Get the error value from EJS
        const errorBox = document.getElementById("errorBox");

        // Check if the error is not an empty string and show the error box
        if (error) {
            errorBox.style.display = "flex";  // Show the error box
        }

        const success = "<%= success %>";
        const successBox=document.getElementById("successBox");
        if (success) {
            successBox.style.display = "flex";  // Show the error box
        }
    </script>
</body>
</html>
