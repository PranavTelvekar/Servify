<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Form</title>
    <!-- Link to Google Fonts for the Roboto family -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Roboto+Condensed:wght@400;700&family=Roboto+Slab:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #8ef2f9, #81a8fb); /* Gradient background */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            font-family: 'Roboto', sans-serif; /* Applying the Roboto font */
            margin: 0;
        }
        .container {
            max-width: 700px;
            background-color: #ffffff;
            box-shadow: 0px 10px 15px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 30px;
            margin: 20px;
            transition: transform 0.2s;
        }
        .container:hover {
            transform: scale(1.01); /* Slight zoom effect */
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #4a00e0;
            font-family: 'Roboto Slab', serif; /* Applying Roboto Slab for headings */
            font-weight: bold;
        }
        .form-group {
            margin-bottom: 20px;
        }
        textarea {
            width: 100%;
            height: 150px;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ced4da;
            resize: none;
            transition: border-color 0.2s;
            font-family: 'Roboto Condensed', sans-serif; /* Applying Roboto Condensed for form elements */
        }
        textarea:focus {
            border-color: #4a00e0;
            outline: none;
        }
        .terms-card {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            border: 1px solid #dee2e6;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.05);
            font-family: 'Roboto', sans-serif;
        }
        .terms-card h5 {
            color: #8e2de2;
            font-family: 'Roboto Slab', serif;
        }
        .btn {
            width: 100%;
            background: linear-gradient(45deg, #b522ef, #fa6b60);
            border: none;
            transition: background 0.2s;
            font-family: 'Roboto Condensed', sans-serif;
        }
        .btn:hover {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
        }
        .btn:disabled {
            background: #a0a5b1;
        }
        .form-check-label {
            cursor: pointer;
            font-family: 'Roboto', sans-serif;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Booking Form</h1>
        <form action="/booking/<%= sid %>" method="post">
            <div class="form-group">
                <label for="problemDescription" class="form-label">Please Tell Us About Your Problem In Detail</label>
                <textarea name="problemDescription" id="problemDescription" class="form-control" required></textarea>
            </div>

            <div class="terms-card">
                <h5>Terms and Conditions: For User</h5>
                <ul>
                    <li>The booking for this slot can be done only once.</li>
                    <li>You can cancel the booked slot at any time if you want.</li>
                    <li>User can book multiple service provider slots if they want.</li>
                    <li>There are no bookings if the previously created slot is deleted by the service provider.</li>
                </ul>
            </div>

            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="agreeTerms">
                <label class="form-check-label" for="agreeTerms">
                    I agree to the Terms and Conditions
                </label>
            </div>

            <button type="submit" class="btn btn-primary" id="submitButton" disabled>Submit and Book</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // JavaScript to enable/disable the button based on checkbox state
        document.getElementById('agreeTerms').addEventListener('change', function() {
            const submitButton = document.getElementById('submitButton');
            submitButton.disabled = !this.checked; // Enable if checked, disable if not
        });
    </script>
</body>
</html>
