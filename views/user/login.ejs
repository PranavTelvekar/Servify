<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            font-size: 30px;
            background-color: #F5F5F5; /* Off White */
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'Lora', serif;
            color: #0D1B2A; /* Rich Black for text */
        }
        /* Navbar */
        .navbar-brand {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            color: #c89f18 !important; /* Accent Color */
            font-weight: bold;
            transition: color 0.3s ease;
        }
        .navbar-brand:hover {
            color: #1F3B4D !important; /* Primary Color */
        }
        .nav-item .nav-link {
            color: #0D1B2A !important; /* Text Color */
            font-size: 1.1rem;
            padding: 8px 20px;
            transition: color 0.3s ease;
        }
        .nav-item .nav-link:hover {
            color: #D4AF37 !important; /* Accent Color */
        }
        .dropdown-item{
            color: #c89f18;
        }

        * Header styling */
        .header {
            width: 100%;
            background: #1F3B4D; /* Deep Navy */
            color: #F5F5F5; /* Off White for contrast */
            padding: 10px 0;
            text-align: center;
            font-family: 'Playfair Display', serif;
        }

        .header h1 {
            font-size: 2rem;
            margin: 0;
        }
        

        /* Header styles */
        .login-header {
            margin-bottom: 30px;
            font-weight: bold;
            color: #1F3B4D; /* Deep Navy */
        }

        /* Button styling */
        .btn-primary {
            width: 100%;
            background: #D4AF37; /* Gold */
            border: none;
            transition: background 0.3s ease;
        }

        .btn-primary:hover {
            background: #B08C2F; /* Darker shade of Gold */
        }

        /* Login Form Container */
        .login-container {
            max-width: 400px;
            margin: 50px auto;
            padding: 40px;
            background-color: #FFFFFF;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            transition: box-shadow 0.3s ease;
        }

        .login-container:hover {
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.2);
        }

        /* Header */
        .login-header {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: bold;
            color: #1F3B4D; /* Primary Color */
            margin-bottom: 30px;
        }

        /* Input Styling */
        .form-control {
            border: 1px solid #A0A7B1;
            border-radius: 8px;
            padding: 10px;
            font-size: 1rem;
        }

        .form-control:focus {
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
            border-color: #007BFF;
        }

        /* Submit Button */
        .btn-primary {
            background-color: #D4AF37;
            border: none;
            font-size: 1.1rem;
            font-weight: 600;
            padding: 12px 25px;
            border-radius: 8px;
            transition: background 0.3s ease, transform 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #A0A7B1;
            transform: scale(1.05);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .login-container {
                padding: 30px;
                margin: 20px;
            }

            .login-header {
                font-size: 1.5rem;
            }

            .btn-primary {
                font-size: 1rem;
                padding: 10px 20px;
            }
        }

    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <!-- Brand -->
            <a class="navbar-brand" href="#">Epic Engineers</a>
    
            <!-- Toggler button for mobile view -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <!-- Collapsible Navbar -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <!-- Login Dropdown -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="loginDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Login</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/user/login">User</a></li>
                            <li><a class="dropdown-item" href="/services/login">Service Provider</a></li>
                        </ul>
                    </li>
                    <!-- Signup Dropdown -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="signupDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Signup</a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/user/signup">User</a></li>
                            <li><a class="dropdown-item" href="/services/signup">Service Provider</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="login-container">
        <h3 class="text-center login-header">Login As User</h3>
        <form action="/user/login" method="POST">
            <!-- Username -->
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input name="username" type="text" class="form-control" id="username" placeholder="Enter Username" required>
            </div>
    
            <!-- Password -->
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input name="password" type="password" class="form-control" id="password" placeholder="Enter Password" required>
            </div>
    
            <!-- Hidden Field -->
            <input type="hidden" name="redirectTo" value="<%= redirectTo %>">
    
            <!-- Submit Button -->
            <button type="submit" class="btn btn-primary w-100">Login</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
