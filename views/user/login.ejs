<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <style>
        body {
            font-size: 30px;
            background-color: #F5F5F5; /* Off White */
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: 'Lora', serif;
            color: #0D1B2A; /* Rich Black for text */
        }

        * Header styling */
        .header {
            width: 100%;
            background: #1F3B4D; /* Deep Navy */
            color: #F5F5F5; /* Off White for contrast */
            padding: 10px 0;
            text-align: center;
            font-family: 'Playfair Display', serif;
        }

        .header h1 {
            font-size: 2rem;
            margin: 0;
        }
        /* Login container styles */
        .login-container {
            width: 25rem;
            height: 25rem;
            margin: 50px auto;
            padding: 20px;
            background-color: #a4c2ed; /* Light Slate Gray */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .login-container:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }

        /* Header styles */
        .login-header {
            margin-bottom: 30px;
            font-weight: bold;
            color: #1F3B4D; /* Deep Navy */
        }

        /* Button styling */
        .btn-primary {
            width: 100%;
            background: #D4AF37; /* Gold */
            border: none;
            transition: background 0.3s ease;
        }

        .btn-primary:hover {
            background: #B08C2F; /* Darker shade of Gold */
        }

        /* Input fields */
        .form-control {
            border: 2px solid #1F3B4D; /* Deep Navy */
            border-radius: 8px;
            transition: all 0.3s;
        }

        .form-control:focus {
            box-shadow: 0 0 8px rgba(31, 59, 77, 0.3);
            border-color: #1F3B4D;
        }

        .form-label {
            font-weight: 500;
            color: #333;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.95);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .login-container {
            animation: fadeIn 0.6s ease-out;
        }

        /* Footer styling */
        .footer {
            width: 100%;
            background: #A0A7B1; /* Light Slate Gray */
            color: #0D1B2A; /* Rich Black */
            padding: 10px;
            text-align: center;
            font-size: 0.9rem;
            position: absolute;
            bottom: 0;
        }

        .footer p {
            margin: 0;
        }
    </style>
</head>
<body>
    <!-- Header with Back Button -->
    <header class="header">
        <h1>Epic Engineers</h1>
        <button class="back-btn" onclick="history.back()">Back</button>
    </header>
    <div class="login-container">
        <h3 class="text-center login-header">Login As User</h3>
        <form action="/user/login" method="POST">
            <!-- Username -->
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input name="username" type="text" class="form-control" id="username" placeholder="Enter Username" required>
            </div>

            <!-- Password -->
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input name="password" type="password" class="form-control" id="password" placeholder="Enter Password" required>
            </div>

            <input type="hidden" name="redirectTo" value="<%= redirectTo %>">

            <!-- Submit Button -->
            <button type="submit" class="btn btn-primary">Login</button>
        </form>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <p>&copy; 2024 Your Company. All rights reserved.</p>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
